<h1><%= t('common.text.title_edit', name: Book.model_name.human) %></h1>

<%= render 'form', book: @book %>

<%= link_to t('common.button.show'), @book %> |
<%= link_to t('common.button.back'), books_path %>
