<!DOCTYPE html>
<html>
  <head>
    <title>BooksApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <p class="navbar-text pull-right">
      <% if user_signed_in? %>
        <%= link_to t('views.common.home'), books_path %> |
        <%= t('views.authentication.logged_in_as', email: current_user.email) %>
        <%= link_to t('views.common.all_list'), users_path %>
        <%= link_to t('views.common.edit'), edit_user_registration_path, class: 'navbar-link' %> |
        <%= link_to t('views.authentication.logout'), destroy_user_session_path, method: :delete, class: 'navbar-link'  %>
      <% end %>
    </p>

    <%= yield %>
  </body>
</html>
